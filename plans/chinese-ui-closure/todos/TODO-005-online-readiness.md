# TODO-005：上线 TODO

当前 TODO 各个要素下的内容留空，根据你迭代认为 Ready 的现状来完成要素填充并持续推进完成验证

- 状态：已完成
- 优先级：P0
- 需求目标：确保当前客户端和真实 Java 后台可稳定承担中文登录、选择、建角及聊天职责。
- 核心实现：以已构建 WASM、当前 Java 编译产物、完整只读 NX/WZ、MySQL、WebSocket/资源/Web 服务组成运行栈，并保留确定性协议验证器及浏览器 E2E。
- 关键要素：不修改运行资源；8484/7575 必须由同一真实 Java 进程监听；数据库使用 utf8mb4；浏览器必须支持 WebGL2；失败通过端口、服务日志、浏览器异常和状态断言直接可见。
- 前置依赖：TODO-001 至 TODO-004，均已完成。
- 当前进展：真实 Java 与所有 Web 依赖运行中；首次建角和持久化复测均通过；未发布或推送远端。
- 验证手段：端口/PID 核对、Java 启动完成标志、双端构建、协议验证器、两轮真实 E2E、数据库 UTF-8 内容查询和截图人工审查。
- 验证证据：真实 Java 同时监听 7575/8484；服务启动输出包含 `MapleStory is now online`；首次建角、持久化、注册入口及角色名副本迁移后复测均 `PASS MapleStory WebUI flow completed` 且浏览器异常为 0；最终证据目录 `/tmp/maplestory-webui-IJXQZN`；数据库保存 `测试一` 的 UTF-8 十六进制值，角色名引用查询无 collation 错误；服务端提交 `e1e9c59b`。
- 下一步：保持服务观察；若异常，先停止当前 Java，再用同一已验证编译产物和只读 WZ 重启；代码回滚使用两个仓库各自提交的父提交，不回滚或删除数据库数据。
- 备注：系统 Chromium 90 不满足 WebGL2，验收使用本机 Chrome for Testing 151；截图位于仓库外，不提交。当前为本地运行就绪，不包含远端推送或生产发布。
