# TODO-001：服务端 UTF-8 与中文角色名

- 状态：已完成
- 优先级：P0
- 需求目标：中文角色名与聊天在客户端、服务器和数据库之间无乱码往返。
- 核心实现：按 UTF-8 字节读写变长字符串；提供固定字节字段写入；服务端验证 3–12 字节 Unicode 字母/数字；迁移 `characters.name`。
- 关键要素：固定字段不拆分码点；12 字节合法角色名在好友申请等所有封包路径可用；ASCII 行为兼容；非法输入明确失败；不删除数据。
- 前置依赖：无。
- 当前进展：严格 UTF-8 编解码、角色名校验和 utf8mb4 迁移已完成；好友申请 11 字节副本按完整 UTF-8 码点截取；真实客户端已创建、重登并显示中文角色，中文聊天已由服务端回显。
- 验证手段：编码边界与好友封包布局测试、Ant 构建、数据库字符集与中文往返查询、真实建角/好友/聊天。
- 验证证据：UTF-8、名字与 Buddy packet verifier 均通过，覆盖 `中文测试` 12 字节边界、完整名字变长字段、11 字节完整码点前缀及 61 字节封包布局；当前服务端源码编译为 1025 个运行类；真实 E2E 创建并重登 `测试一`，数据库 `HEX(name)=E6B58BE8AF95E4B880`、3 字符/9 字节；`characters.name` 及便笺、戒指、礼物等角色名副本列均为 utf8mb4_unicode_ci，`notes.to='测试一'` 查询无 collation 错误；截图 `09-chinese-chat.png` 显示服务端回显中文聊天。
- 下一步：无。
- 备注：账号名 `test1` 保持 ASCII。
